# Dashboard local config (copy to .env.local, never commit secrets).
NEXT_PUBLIC_API_URL=http://localhost:8787
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=
# Dev login (LOCAL ONLY — stub, not a user DB; login page pre-fills these)
# ALLOW_DEV_LOGIN=true
# DEV_LOGIN_EMAIL=admin@agentledger.local
# DEV_LOGIN_PASSWORD=ledger-dev
